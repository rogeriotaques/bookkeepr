# Commands

```sh
# Backend (runs on port 8083)
(cd backend; yarn start)

# Frontend (runs on port 5173)
(cd frontend; yarn start)

# Build frontend for production
(cd frontend; yarn build)

# Watch mode for frontend builds
(cd frontend; yarn watch)
```